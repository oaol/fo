allprojects  {
  apply plugin: 'maven'

  group = 'com.fo'
  version = '1.0.0'
}

subprojects {
  apply plugin: 'java'
  tasks.withType(JavaCompile) {
  	options.encoding = 'UTF-8'
  }

  
  repositories {
    mavenCentral()
  }
  
  
}